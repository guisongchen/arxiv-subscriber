[project]
name = "arxiv-subscriber"
version = "0.1.0"
description = "Subscribe to arXiv CS papers and sync to Notion"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx[socks]>=0.28.1",
    "openai>=1.0.0",
    "python-dotenv>=1.2.1",
]
